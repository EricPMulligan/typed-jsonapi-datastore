{
  "private": true,
  "scripts": {
    "build": "echo building... && typings bundle -o out/dist/node-jsonapi-datastore.d.ts",
    "lint": "echo linting... && tslint \"**/*.ts\" -e \"source/**\" -e \"source-test/**\" -e \"out/**\" -e \"node_modules/**\" -e \"typings/**\"",
    "browser-test": "echo running browser tests... && echo no browser test",
    "source-test": "echo running source tests... &&  echo no source test",
    "test": "echo running server tests... && cd test && ../node_modules/.bin/mocha --require ts-node/register **/*.spec.ts",
    "watch": "onchange -w \"**/*.ts\" -i -e \"out/**\" -- npm -s run build+test",
    "publish": "npm -s run lint+build+test && echo please publish to typings/registry",
    "all-tests": "npm test",
    "build+test": "npm run build && npm run all-tests",
    "lint+build+test": "npm run lint && npm run build+test",
    "prepublish": "typings install"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "jsonapi-datastore": "0.4.0-beta",
    "mocha": "^2.5.3",
    "onchange": "^2.5.0",
    "ts-node": "^0.9.1",
    "tslint": "^3.11.0",
    "tslint-config-typings": "^0.2.3",
    "typings": "^1.2.0"
  }
}
